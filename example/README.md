# Axum Autoroute Example

Example and test application for `axum-autoroute`.
It contains:
* A set of different routes testing different features of `axum-autoroute` (extractors, responses, etc.) and the associated unit tests.
* A binary exposing the different routes and a swagger-ui interface.
* Trybuild tests the error messages returned by the `autoroute` macro.
